const state = { token: sessionStorage.getItem('remote-reply-token') || '' };
function headers() { return { 'content-type': 'application/json', ...(state.token ? { authorization: `Bearer ${state.token}` } : {}) }; }
async function request(path, options = {}) { const response = await fetch(path, { ...options, headers: { ...headers(), ...(options.headers || {}) } }); if (response.status === 401) { const token = window.prompt('Enter CHATBOT_ACCESS_TOKEN from your private config.ini:'); if (token === null) throw new Error('Access token is required.'); state.token = token.trim(); sessionStorage.setItem('remote-reply-token', state.token); return request(path, options); } const body = await response.json(); if (!response.ok) throw new Error(body.error || `Request failed with HTTP ${response.status}.`); return body; }
function values() { return Object.fromEntries([...document.querySelectorAll('[data-setting]')].map(input => [input.dataset.setting, input.value.trim()])); }
async function loadSettings() { const status = document.querySelector('#settings-status'); try { const { settings } = await request('/api/settings'); for (const input of document.querySelectorAll('[data-setting]')) input.value = settings[input.dataset.setting] || ''; status.textContent = 'Loaded current values from config.ini.'; } catch (error) { status.textContent = error.message; } }
document.querySelector('#settings-reload').addEventListener('click', loadSettings);
document.querySelector('#settings-form').addEventListener('submit', async event => { event.preventDefault(); const button = event.currentTarget.querySelector('button[type="submit"]'); const status = document.querySelector('#settings-status'); button.disabled = true; status.textContent = 'Saving…'; try { status.textContent = (await request('/api/settings', { method: 'POST', body: JSON.stringify({ settings: values() }) })).message; } catch (error) { status.textContent = error.message; } finally { button.disabled = false; } });
loadSettings();
